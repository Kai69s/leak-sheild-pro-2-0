{
  "name": "leak-sheild-pro-2-0",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
