{
  "name": "leak-shield-pro",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
